# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Only include the bin folder when downloading from the Asset Library.
/**        export-ignore
/bin    !export-ignore
/bin/** !export-ignore